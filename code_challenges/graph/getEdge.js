import {Vertex,Edge,Graph} from "./graph.js"

function getEdge(graph,array){
    let weightTotal = 0;
    for (let i=0;i<array.length;i++){
        // i has a direct edge to i+1
        // weightTotal + weight of edge
        // else return false
    }
}